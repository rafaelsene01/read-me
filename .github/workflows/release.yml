name: Release

# Manual only. There is deliberately no `push`, `tag` or `schedule` trigger in
# this file — "a push never publishes a release" is structural, not a convention
# someone has to remember.
on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Tipo de incremento da versão"
        required: true
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  prepare:
    name: Versão, changelog e tag
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
      notes: ${{ steps.notes.outputs.notes }}
      release_sha: ${{ steps.commit.outputs.sha }}
    steps:
      # Guard first, before the checkout: nothing should be written if this is
      # the wrong branch.
      - name: Guarda — releases só saem de master
        if: github.ref_name != 'master'
        run: |
          echo "::error::Release só pode ser disparada a partir de master (veio de '${{ github.ref_name }}')"
          exit 1

      - uses: actions/checkout@v5
        with:
          fetch-depth: 0 # version calculation and changelog need the full history

      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm

      - name: Calcular a próxima versão
        id: version
        run: |
          set -euo pipefail
          git fetch --tags --force

          LAST_TAG=$(git tag --list 'v*' --sort=-v:refname | head -n 1)
          if [ -z "$LAST_TAG" ]; then
            # No tag yet: the version currently in package.json is treated as the
            # last released one, so the first release is a bump over it.
            BASE=$(node -p "require('./package.json').version")
            echo "Nenhuma tag encontrada; usando package.json como base ($BASE)"
          else
            BASE=${LAST_TAG#v}
            echo "Última tag: $LAST_TAG (base $BASE)"
          fi

          VERSION=$(node scripts/bump-version.mjs "${{ inputs.bump }}" --base "$BASE" --dry-run)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"
          echo "Nova versão: $VERSION"

      - name: Guarda — a tag não pode já existir
        run: |
          set -euo pipefail
          TAG="${{ steps.version.outputs.tag }}"
          if git rev-parse -q --verify "refs/tags/$TAG" >/dev/null; then
            echo "::error::A tag $TAG já existe localmente"
            exit 1
          fi
          if git ls-remote --exit-code --tags origin "refs/tags/$TAG" >/dev/null 2>&1; then
            echo "::error::A tag $TAG já existe no remoto"
            exit 1
          fi

      - name: Gravar a versão nos arquivos que a duplicam
        run: node scripts/bump-version.mjs "${{ steps.version.outputs.version }}"

      - uses: dtolnay/rust-toolchain@stable

      - name: Atualizar Cargo.lock
        # `cargo metadata` refreshes the lockfile without building anything, so
        # this job needs neither protoc nor the webkit system dependencies.
        run: cargo metadata --manifest-path src-tauri/Cargo.toml --format-version 1 > /dev/null

      - name: Gerar CHANGELOG.md
        uses: orhun/git-cliff-action@v4
        with:
          config: cliff.toml
          args: --tag ${{ steps.version.outputs.tag }}
        env:
          OUTPUT: CHANGELOG.md

      - name: Gerar as notas desta versão
        uses: orhun/git-cliff-action@v4
        id: cliff_notes
        with:
          config: cliff.toml
          args: --tag ${{ steps.version.outputs.tag }} --unreleased --strip all
        env:
          OUTPUT: RELEASE_NOTES.md

      - name: Expor as notas como output
        id: notes
        run: |
          {
            echo 'notes<<RELEASE_NOTES_EOF'
            cat RELEASE_NOTES.md
            echo 'RELEASE_NOTES_EOF'
          } >> "$GITHUB_OUTPUT"
          rm -f RELEASE_NOTES.md

      - name: Commitar e taguear
        id: commit
        run: |
          set -euo pipefail
          git config user.name  'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          # tauri.conf.json is absent on purpose: its version is "../package.json".
          git add package.json package-lock.json src-tauri/Cargo.toml src-tauri/Cargo.lock CHANGELOG.md
          git commit -m "chore(release): ${{ steps.version.outputs.tag }}"
          git tag -a "${{ steps.version.outputs.tag }}" -m "${{ steps.version.outputs.tag }}"
          git push origin HEAD:master
          git push origin "${{ steps.version.outputs.tag }}"
          # Handed to `cleanup`, which reverts exactly this commit if the run
          # never reaches a published release.
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

  build:
    name: Empacotar (${{ matrix.os }})
    needs: prepare
    # One platform failing must not cancel the other: a half-built draft is
    # still salvageable, a cancelled one is not.
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            bundles: msi,nsis
          - os: ubuntu-22.04
            bundles: deb,appimage
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
        with:
          # Build from the tag, so the artifact matches exactly what was tagged.
          ref: ${{ needs.prepare.outputs.tag }}

      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Dependências de sistema (Linux)
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            libgtk-3-dev \
            libxdo-dev \
            libssl-dev \
            build-essential \
            file \
            patchelf \
            libfuse2 \
            wget \
            curl \
            protobuf-compiler

      - name: protoc (Windows)
        if: matrix.os == 'windows-latest'
        run: choco install protoc -y

      - run: npm ci

      - name: Build + bundle
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # linuxdeploy strips every ELF it deploys, and it aborts the whole
          # AppImage when `strip` refuses one of them. Since the M9 the AppDir
          # carries 256 MB of third-party binaries — two llama-server builds,
          # ONNX Runtime and pdfium — that nothing here compiled.
          #
          # The correlation is in the published artifacts, not in a guess:
          # v0.2.0 predates `bundle.resources` and its AppImage is on the
          # release; v0.3.0 is the first build to ship the vendored tree and the
          # first to die in linuxdeploy, with the .deb of the same run bundling
          # fine (the .deb does not relocate or strip anything).
          #
          # Stripping them buys nothing anyway: the app's own binary is already
          # stripped by the release profile (REL-27, measured at −26,7%), and
          # AD-046 is the standing reminder that touching these vendored files
          # is how the runtime stopped executing last time.
          NO_STRIP: "true"
        with:
          tagName: ${{ needs.prepare.outputs.tag }}
          releaseName: ReadMe ${{ needs.prepare.outputs.tag }}
          releaseBody: ${{ needs.prepare.outputs.notes }}
          # Stays a draft until `finalize` confirms every artifact landed.
          releaseDraft: true
          prerelease: false
          # `--verbose` so a future linuxdeploy failure arrives with the reason
          # attached. The v0.3.0 log had `failed to run linuxdeploy` and not one
          # line from linuxdeploy itself, which is why this had to be diagnosed
          # from published artifacts instead of from the error.
          args: --bundles ${{ matrix.bundles }} --verbose

      # Records whether the .deb keeps the execute bit on the vendored
      # llama-server (Open Question #1 of the self-contained-runtime design).
      # A missing binary fails the build; a missing bit only prints, because
      # `runtime::bundled::ensure_executable` repairs that at run time.
      - name: Conferir os componentes dentro do .deb
        if: matrix.os == 'ubuntu-22.04'
        shell: bash
        run: |
          set -euo pipefail
          DEB=$(find src-tauri/target/release/bundle/deb -name '*.deb' | head -n 1)
          node scripts/check-linux-bundle.mjs "$DEB"

      # Tauri has no portable target and the official updater has no portable
      # path on Windows, so this bundle is assembled and signed by hand — with
      # the same key, so both update paths share one trust root.
      - name: Bundle portátil (Windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          ZIP=$(node scripts/make-portable.mjs --version "${{ needs.prepare.outputs.version }}")
          echo "Bundle portátil: $ZIP"
          npx tauri signer sign "$ZIP"
          gh release upload "${{ needs.prepare.outputs.tag }}" "$ZIP" "$ZIP.sig" --clobber

  finalize:
    name: Publicar a release
    needs: [prepare, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ needs.prepare.outputs.tag }}

      - uses: actions/setup-node@v5
        with:
          node-version: 24

      - name: Injetar a entrada portátil no latest.json
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          TAG="${{ needs.prepare.outputs.tag }}"
          VERSION="${{ needs.prepare.outputs.version }}"
          ZIP_NAME="ReadMe_${VERSION}_x64-portable.zip"

          gh release download "$TAG" -p latest.json -p "${ZIP_NAME}.sig" --clobber
          # The asset URL is read from the release, never rebuilt from a guess:
          # Tauri's artifact naming varies between versions and locales.
          gh release view "$TAG" --json assets --jq '.assets' > assets.json

          # `--tag` is what makes the URL usable: the release is still a draft
          # here, so the asset URL read above points at the ephemeral
          # `untagged-<hash>` path that dies when the release is published.
          node scripts/patch-latest-json.mjs \
            --manifest latest.json \
            --key windows-x86_64-portable \
            --tag "$TAG" \
            --assets assets.json \
            --name "$ZIP_NAME" \
            --signature-file "${ZIP_NAME}.sig"

          gh release upload "$TAG" latest.json --clobber

      - name: Publicar (sair de rascunho)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release edit "${{ needs.prepare.outputs.tag }}" --draft=false

  # `prepare` pushes the tag and the version commit before anything is built,
  # so a build that fails — or that someone cancels — used to leave a tag
  # pointing at a release that does not exist, and a version number burned:
  # the next run would compute the one after it. Happened for real on the first
  # release attempt (v0.1.1, cancelled at 29 min). This job puts the repository
  # back where it was, so retrying reuses the same number.
  cleanup:
    name: Desfazer uma release interrompida
    needs: [prepare, build, finalize]
    # Only when `prepare` actually wrote something and the run did not end in a
    # published release. `always()` is required for the job to run at all after
    # a failed or cancelled dependency.
    if: >-
      always()
      && needs.prepare.result == 'success'
      && (needs.build.result != 'success' || needs.finalize.result != 'success')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: master
          fetch-depth: 0 # the revert needs the release commit in history

      - name: Apagar a release e a tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          TAG="${{ needs.prepare.outputs.tag }}"

          # The draft goes first: deleting the tag underneath a release leaves
          # the release behind, orphaned and undeletable by tag.
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release delete "$TAG" --yes
            echo "Release $TAG apagada"
          else
            echo "Nenhuma release $TAG para apagar"
          fi

          git push origin ":refs/tags/$TAG" || echo "::warning::A tag $TAG já não existia no remoto"

      - name: Reverter o commit de versão
        run: |
          set -euo pipefail
          SHA="${{ needs.prepare.outputs.release_sha }}"
          git config user.name  'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'

          # Revert, never force-push: master is a published branch and rewriting
          # it would break every clone. A revert commit is ugly and safe.
          if git revert --no-edit "$SHA"; then
            git push origin HEAD:master
            echo "Commit de versão $SHA revertido"
          else
            git revert --abort || true
            echo "::error::Não foi possível reverter $SHA automaticamente (provavelmente houve outro push em master). A versão em package.json continua adiantada — reverta na mão antes da próxima release."
            exit 1
          fi
